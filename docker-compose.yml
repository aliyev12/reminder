version: '3.8'

services:
  reminder-app:
    build: .
    container_name: go-reminder-scheduler
    ports:
      - "8080:8080"
    env_file:
      - .env
    volumes:
      - ./data:/root/
    environment:
      - TZ=America/New_York